/* day6 - 구조체, 구조체배열, 구조체포인터 */

//# include <stdio.h>

//------------------------------------------------------------------------------------------------------------------------ 구조체 

// 서로 연관있는 여러 데이터를 하나로 묶어 사용하는 자료형으로 서로다른 타입의 데이터 다룰 때 사용함 

//#include <stdio.h>
//
//struct Point {
//    int x;
//    int y;
//};
//
//int main(void) {
//    struct Point pt = { .x = 3, .y = 4 };
//    printf("Point: (%d, %d)\n", pt.x, pt.y);
//    return 0;
//}

//------------------------------------------------------------------------------------------------------------------------ 구조체 배열

// 같은 타입의 구조체를 여러 개 묶은 배열

//#include <stdio.h>
//
//struct Student {
//    char name[20];
//    int score;
//};
//
//int main(void) {
//    struct Student cls[3] = {
//        { "Alice", 90 },
//        { "Bob",   85 },
//        { "Carol", 95 }
//    };
//
//    for (int i = 0; i < 3; i++) {
//        printf("%s: %d점\n", cls[i].name, cls[i].score);
//    }
//    return 0;
//}

//------------------------------------------------------------------------------------------------------------------------ 구조체 포인터

// 구조체를 가리키는 포인터로 동적할당된 구조체나 함수에 구조체를 참조로 넘길 때 사용 -> 메모리효율적관리, 함수호출시 복사대신 참조 전달의 이득있음 

//#include <stdio.h>
//#include <stdlib.h>
//
//struct Student {
//    char name[20];
//    int score;
//};
//
//int main(void) {
//    struct Student* p = malloc(sizeof * p);
//    if (!p) return 1;
//
//    // 값 입력
//    snprintf(p->name, sizeof p->name, "David");
//    p->score = 88;
//
//    printf("%s: %d점\n", p->name, p->score);
//    free(p);
//    return 0;
//}

//------------------------------------------------------------------------------------------------------------------------ 구조체 vs 함수 vs 객체 

//구분					구조체						함수					객체(OOP)
//역할				데이터 묶음					동작 묶음				데이터 + 동작 묶음
//C 언어에서		struct 키워드				returnType name()			직접 지원 안 함 → struct + 함수 조합으로 흉내
//특징			여러 변수 필드를 가짐		입력(인자)→출력(반환)		속성(멤버 변수) + 메서드(함수) 포함
//예시			struct Point { … };		int add(int, int)			class Student { … };

// 즉 구조체는 데이터만 묶은 그룹이고 함수는 동작을 수행하는거고 객체는 두개 같이하는거

//------------------------------------------------------------------------------------------------------------------------ 프로젝트

// ay5 project를 구조체로 바꾸기

//------------------------------------------------------------------------------------------------------------------------ 백준 

// 10814(나이순 정렬) , 10825(국영수) , 11650(좌표정렬) 