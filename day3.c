/* day3 - 조건문, and/or, switch, enum, 함수, 배열, 문자열 */

//#include <stdio.h>
//#include <string.h>*/ // 문자열 관련 함수 사용하기 위한 헤더파일  


//------------------------------------------------------------------------------------------------------------------------ 조건문

// if
// 조건이 참이면 코드를 실행하고 거짓이면 실행하지 않음
// 기본형태

//if (조건) {
//	실행 코드;
//}

//int main() {
//	int a = 10; 
//	
//	if (a > 0) {
//		printf("a는 0보다 크다\n");
//	}
//	return 0;
//}

// if-else
// 조건이 참이면 if문, 거짓이면 else문 실행 
// 기본형태

//if (조건) {
//	실행코드;
//}
//else {
//	실행코드;
//}

//int main() {
//	int a = 10;
//	if (a < 0) {
//		printf("a는 0보다 작다");
//	} else {
//		printf("a는 0보다 크다");
//	}
//	return 0;
//}

// if-else 여러번 실행

//int main() {
//    int score = 85;
//
//    if (score >= 90) {
//        printf("A\n");
//    }
//    else if (score >= 80) {
//        printf("B\n");
//    }
//    else if (score >= 70) {
//        printf("C\n");
//    }
//    else {
//        printf("F\n");
//    }
//    return 0;
//}

// if문을 쓰면 되는데 왜 if-else를 사용할까? ==> 효율성
// 만약 if가 5개 있다고 치자 그럼 컴퓨터는 if를 다 봐야해서 5번 돌아감 
// 이대신 else-if 를 사용하면 조건에 만족하는 순간 else로 가기때문에 if를 5변 안봐도 됨 

// cf) 삼항연산자라고 if문 대체하는 거 있는데 이거는 자율적으로 궁금하면 공부해보세요 - https://codevang.tistory.com/40

// 조건문 중첩

//int main() {
//    int a = 8;
//
//    if (a <= 10) {
//        if (a%3==0) {
//            printf("a는 10보다 작으며 3의 배수이다 \n");
//        }
//        else {
//            printf("a는 10보다 작으며 3의 배수가 아니다\n");
//        }
//    }
//    else {
//        printf("a는 10보다 크다\n");
//    }
//
//    return 0;
//}

//------------------------------------------------------------------------------------------------------------------------ and, or

//복습 - 논리연산자 )
//1. &&	AND : 둘 다 참이면 참	
//2. ||	OR : 둘 중 하나라도 참이면 참

//조건문에서 논리연산자 사용 

//int main() {
//    int a;
//    for (a = 1; a < 50; a++) {
//        if (a % 3 == 0 || a % 4 == 0) {              // cf) 이게 왜 성립하냐? -> 연산자의 우선순위가 관계연산자인 == 이 더 높아서 가능 *이거도 혼자 찾아보고 아 이렇구나 하고 넘어가기
//            printf("3또는 4의 배수 : %d\n", a);
//        }
//    }
//    return 0;
//}

//------------------------------------------------------------------------------------------------------------------------ switch

//변수의 값에 따라 여러 경우를 분기처리하는 제어문 (그냥 단순하게 생각하면 if-if else 여러개 쓸거를 얘로 가독성있게 정리한거)
//
//기본형태
//
//switch (변수) {
//case 값1:
//	실행코드;
//	break;
//case 값1:
//	실행코드;
//	break;
//dafault:
//	모든 case 아닐떄 실행코드;
//	break;
//}

//int main() {
//    int menu;
//
//    printf("메뉴 선택 (1: 콜라, 2: 사이다, 3: 커피): ");
//    scanf("%d", &menu);
//
//    switch (menu) {
//    case 1:
//        printf("콜라\n");
//        break;
//    case 2:
//        printf("사이다\n");
//        break;
//    case 3:
//        printf("커피\n");
//        break;
//    default:
//        printf("잘못된 입력\n");
//        break;
//    }
//    return 0;
//}

// break를 사용해서 좋은 경우도 있지만 안써서 유용한 경우도 있기때문에 swtich코드를 짤때 고민을 해야함 

//------------------------------------------------------------------------------------------------------------------------ enum을 사용한 switch

//enum : 련된 정수 상수에 이름을 붙여서 사용할 수 있게 해주는 사용자 정의 자료형
//
//기본형태
//
//enum 열거형이름 {
//    값1, 값2, 값3, ...
//};

//enum Weekday {
//    MON,  // 0
//    TUE,  // 1
//    WED,  // 2
//    THU,  // 3
//    FRI,  // 4
//    SAT,  // 5
//    SUN   // 6
//};
//
//int main() {
//    enum Weekday today = WED;
//
//    if (today == WED) {
//        printf("수요일\n");
//    }
//    return 0;
//}

//enum Weekday { MON = 1, TUE, WED, THU, FRI, SAT, SUN };
//
//int main() {
//    enum Weekday today = FRI;
//
//    switch (today) {
//    case MON:
//    case TUE:
//    case WED:
//    case THU:
//    case FRI:                   // 이렇게 케이스 여러개 묶을수도 있음 
//        printf("평일\n");
//        break;
//    case SAT:
//    case SUN:
//        printf("주말\n");
//        break;
//    default:
//        printf("잘못된 입력\n");
//        break;
//    }
//    return 0;
//}

//------------------------------------------------------------------------------------------------------------------------ swtich vs if-else if

//						switch 문					if - else if 문
//사용가능 타입	정수(int, char, enum)	모든 타입 가능(int, float, string, etc.)
//가독성				많을수록 깔끔함					많아지면 복잡해짐
//범위 조건				불가능						가능(if (x > 10))
//연산			불가능(case x + 1 같은 표현 X)		가능(if (x % 2 == 0))
//break 필요 여부		필요								필요 없음

//------------------------------------------------------------------------------------------------------------------------ 함수
// 
//특정 잡업을 수행하는 코드 블록으로 한번만 정의해두고 필요할때마다 호출해서 반복적으로 사용 가능 
//
//기본형태
//반환형 함수이름(매개변수목록) {
//    실행할 코드;
//    return 반환값;
//}
//
//함수는 매개변수가 있고없고, 반환값이 있고없고 이 두개의 조합으로 4가지 유형이 있음
//
//1. 매개변수o 반환값o
//
//int add(int a, int b) {
//    return a + b;
//}
//int main() {
//    int result = add(3, 7);
//    printf("결과: %d\n", result);  
//    return 0;
//}

//
//2. 매개변수o 반환값x

//void greet(char name[]) {
//    printf("안녕하세요 %s님!\n", name);  // 단순한 출력이라 반환값이 필요 없음 
//}
//int main() {
//    greet("철수");
//    return 0;
//}
//
//3. 매개변수x 반환값x
//
//void printWelcome() {
//    printf("환영합니다\n");
//}
//int main() {
//    printWelcome();
//    return 0;
//}

//4. 매개변수x 반환값o

//int getAnswer() {
//    return 42;
//}
//int main() {
//    int answer = getAnswer();
//    printf("정답은: %d\n", answer);
//    return 0;
//}

//c언어는 기본족으로 값에의한전달 즉 call by value 이다 - 함수 안에서 값을 바꾸어도 원본에는 이상 없고 원본을 바꾸고 싶다면 포인터를 사용 -> 나중에 다시 배울예정  그냥 아 그렇구나 하고 지금은 넘기기

//------------------------------------------------------------------------------------------------------------------------배열 

//같은 자료형의 값을 여러 개 저장할 수 있는 공간으로 각 값은 index를 통해 접근
//
//int scores[5]; // 정수 5개 저장할 배열
//char letters[10]; // 문자 10개 저장할 배열
//int numbers[3] = { 1, 2, 3 };  // 선언과 동시에 초기화
//
//printf("%d\n", numbers[1]); // 두 번째 요소 출력
//
//배열은 크기가 정적인 특징이 있다

//------------------------------------------------------------------------------------------------------------------------ 문자열 

//c언어에서 문자열을 위한 정해진 자료형은 없어서 문자를 배열로 이용해서 문자열로 표현 
//
//char str[] = "Hello";
//
//문자열의 index -> 0=H 1=e 2=l 3=l 4=o 5=\0 (널문자라고 문자열 끝을 알리는 것)

//------------------------------------------------------------------------------------------------------------------------ 배열과 문자열의 관계

//문자열 char str[] = "Hello"; 이거는 사실상 
//char str[] = { 'H', 'e', 'l', 'l', 'o', '\0' }; 이런 배열과 동치 
//
//즉 단순하게 문자형 배열 = 문자열 
//
//							배열					문자열(문자 배열)
//정의			같은 자료형의 값 묶음			char형 배열 + 널 문자 \0
//사용 목적	여러 값을 하나의 이름으로 저장		문자의 집합을 의미 있는 문자열로
//자료형			int, float, char 등 가능		오직 char 배열만 문자열로 사용 가능
//끝의 기준		크기만큼 반복						\0이 나올 때까지가 문자열 끝

//int main() {
//    int nums[3] = { 1, 2, 3 };       
//    char word[] = "Hi";             
//
//    printf("숫자 배열: %d %d %d\n", nums[0], nums[1], nums[2]);
//    printf("문자열: %s\n", word);   
//    printf("문자 배열 개별 접근: %c %c\n", word[0], word[1]); 
//
//    return 0;
//}

//------------------------------------------------------------------------------------------------------------------------ 프로젝트
//1. 단어 맞추기 게임 (자세한 내용 수업때 이야기) 
// hint

//1. strcmp() : 두 문자열이 같은지 비교하는 함수로 같으면0 아니면 다른값 반환  (주소가 아닌 내용을 비교)
//if (strcmp(answer, word) == 0) {
//    printf("정답입니다!\n\n");
//
//2. 다차원개념이 조금 들어가서 
//char words[MAX_WORDS][WORD_LEN] = { "apple", "banana", "grape" };
//char hints[MAX_WORDS][WORD_LEN] = { "a _ _ l e", "b a _ _ _ a", "g _ _ _ e" };
//int score = 0;
//
//이코드 그냥 main에 그대로 사용 

//3. 맞추어야할 단어 갯수랑 길이 
//#define MAX_WORDS 3
//#define WORD_LEN 20 
//이걸로 사용 
// 왜 int로 선언 안하고 define으로 했냐면 배열 크기에 사용할 수 있는 정수상수이기때문  - 자세한건 자율학습

//------------------------------------------------------------------------------------------------------------------------ 백준

// 10807(개수 세기), 10871(x보다 작은 수), 10818(최소,최대), 2577(숫자의 개수), 1546(평균), 15596(정수 N개의 합), 1065(한수), 9086(문자열), 14681(사분면 고르기), 10817(세 수), 2884(알람 시계)